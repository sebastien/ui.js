<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" media="screen" href="./uijs.xsl.xml"?>
<Test>
	<title>Path Parsing</title>
	<Case>
		const {parsePath, composePaths} = await import(`${path.js}/ui/paths.js`);
		test(({expect})=>{
			expect(parsePath("@"), ["@"]);
			expect(parsePath("a.b.c"), ["a", "b", "c"]);
			expect(composePaths(parsePath("a.b.c"), parsePath("@")), ["a", "b", "c"]);
		});
	</Case>
</Test>
