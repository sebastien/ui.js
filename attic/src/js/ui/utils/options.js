const Options = {
	debug: false,
	anchors: false,
	allowDuplicateTemplates: false,
	exportState: true,
	locale: globalThis?.Intl.DateTimeFormat().resolvedOptions().locale,
};
export default Options;
// EOF
